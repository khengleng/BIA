# Boutique Advisory Platform Configuration

# Server Configuration
PORT=4000
NODE_ENV=development

# Database Configuration (using existing CM PostgreSQL)
DATABASE_URL="postgresql://postgres:password@localhost:1002/boutique_advisory?schema=public"

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=24h

# AWS S3 Configuration
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=us-east-1
AWS_S3_BUCKET=boutique-advisory-documents

# Redis Configuration (using existing CM Redis)
REDIS_URL=redis://localhost:1003

# External Service Integration

# DID Infrastructure
DID_API_GATEWAY_URL=http://localhost:8080
DID_VC_SERVICE_URL=http://localhost:8080/vc
DID_WORKFLOW_ENGINE_URL=http://localhost:8080/workflow
DID_ATTESTATION_SERVICE_URL=http://localhost:8080/attestation

# CM Infrastructure
CM_PORTAL_URL=http://localhost:3000
CM_KEYCLOAK_URL=http://localhost:8083
CM_VAULT_URL=http://localhost:8200
CM_POSTGRES_HOST=localhost
CM_POSTGRES_PORT=5432

# RWA Infrastructure
RWA_API_URL=http://localhost:9000
RWA_INVESTOR_APP_URL=http://localhost:9002
RWA_ISSUER_CONSOLE_URL=http://localhost:9001

# Temporal Workflow Engine
TEMPORAL_SERVER_URL=localhost:7233

# KYC/AML Service (mock for now)
KYC_SERVICE_URL=http://localhost:1005/kyc

# Multi-language Support
DEFAULT_LANGUAGE=en
SUPPORTED_LANGUAGES=en,km,zh

# Multi-tenant Configuration
DEFAULT_TENANT_ID=default
TENANT_ISOLATION_ENABLED=true
