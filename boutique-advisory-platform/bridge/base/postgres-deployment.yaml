#! postgres-deployment.yaml
# Generated code, do not edit
apiVersion: apps/v1
kind: Deployment
metadata:
    name: postgres
    namespace: boutique-advisory-platform
    labels:
        com.docker.compose.project: boutique-advisory-platform
        com.docker.compose.service: postgres
spec:
    replicas: 1
    selector:
        matchLabels:
            com.docker.compose.project: boutique-advisory-platform
            com.docker.compose.service: postgres
    strategy:
        type: Recreate
    template:
        metadata:
            labels:
                com.docker.compose.project: boutique-advisory-platform
                com.docker.compose.service: postgres
                com.docker.compose.network.bia-network: "true"
        spec:
            restartPolicy: unless-stopped
            containers:
                - name: bia-postgres
                  image: bia-postgres:latest
                  imagePullPolicy: IfNotPresent
                  env:
                    - name: POSTGRES_DB
                      value: "bia_development"
                    - name: POSTGRES_PASSWORD
                      value: "bia_secret_password"
                    - name: POSTGRES_USER
                      value: "bia_user"
                  livenessProbe:
                    exec:
                        command:
                            - /bin/sh
                            - -c
                            - pg_isready -U bia_user -d bia_development
                    periodSeconds: 10
                    timeoutSeconds: 5
                    failureThreshold: 5
                  ports:
                    - name: postgres-5432
                      containerPort: 5432
                  volumeMounts:
                    - name: var-lib-postgresql-data
                      mountPath: /var/lib/postgresql/data
            volumes:
                - name: var-lib-postgresql-data
                  persistentVolumeClaim:
                    claimName: postgres-postgres-data
